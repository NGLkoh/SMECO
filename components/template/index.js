import React from 'react'
import { Flex, Box, useColorModeValue, Image, Stack, Text, InputRightElement, InputLeftElement, Input, InputGroup} from '@chakra-ui/react'
import Navbar from '../nabvar'

export default function Template({children}) {
	return (<Box>
     <Navbar/>
</Box>)
}